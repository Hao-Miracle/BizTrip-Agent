# BizTrip Agent 产品原则

BizTrip Agent 是面向个人商务差旅的本地优先报销代理。用户指定报销时间后，Agent 从邮箱收集行程和票据，理解费用，恢复行程关系，检查缺失、重复与冲突，最后交付经过核验的 Excel 和报销原件。

## 核心价值

用户需要的不是“把邮件导出成表格”，而是确认本次报销的费用和凭证是否完整、正确、可以提交。

## 系统边界

- **体检 Skill**：复用宿主 Agent 的模型，给出金额、分类、行程和材料缺口，不生成最终报销包。
- **Agent 模型**：理解复杂邮件、提取结构化字段、辅助证据判断和行程聚合。
- **本地确定性引擎**：读取 IMAP、解析附件、计算金额、检查证据、阻止错误交付、生成报销包。

Skill 不复制引擎的识别和计算逻辑，也不能调用完整交付接口；模型不能绕过确定性质量门槛。

## 工作闭环

1. 用户指定报销时间范围。
2. 引擎通过只读 IMAP 收集相关邮件和原件。
3. 模型与规则协同完成分类、提取和行程聚合。
4. 确定性校验检查金额、日期、供应商、原件、重复、冲突和行程归属。
5. 证据不足时只询问用户必须确认的信息。
6. 所有问题解决后，交付最终 Excel 和本次引用的原件。

## 准确性原则

- 不猜测金额、日期、供应商、票据编号或行程归属。
- 模型输出必须能回到邮件或原件证据。
- 发现缺失或冲突时停止交付，不用一份看似完整的错误表格掩盖问题。
- 只有质量门槛通过后，任务状态才能是 `completed`。

## 隐私原则

- 邮箱授权码和模型 API Key 仅保存在用户电脑，Skill 不读取或展示它们。
- 原始附件、任务状态和报销包保存在本地。
- 使用云端模型时，必要的邮件和票据文本会发送到用户配置的模型服务。
- 需要数据完全不出本机时，用户应配置兼容 OpenAI 协议的本地模型。

## 不做什么

- 不修改、删除或发送用户邮件。
- 不代替企业或财务人员做最终审批。
- 不宣称已完成尚未接入的发票验真、企业制度或多用户能力。
- 不把内部 JSON、缓存和中间文件当作普通用户交付物。
